# Disable ModSecurity for monitoring.charles.work.gd
# Grafana API queries contain legitimate JSON/PromQL that triggers false positives

SecRule REQUEST_HEADERS:Host "@rx ^monitoring\.charles\.work\.gd$" \
    "id:9999001,\
    phase:1,\
    pass,\
    nolog,\
    ctl:ruleEngine=Off"
